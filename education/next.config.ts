import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["wp.yuvabeeducation.com", "yuvabe-education-wordpress.local"],
  },
};

export default nextConfig;
